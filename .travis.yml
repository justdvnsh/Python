language: python
python:
    - "3.5"
    - "nightly"
notifications:
    - email: false
before_install:
    - pip install pep8
install:
    - pip install -r requirements.txt
script:
    - pytest --verbose
